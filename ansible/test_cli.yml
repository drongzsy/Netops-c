- hosts: all
  gather_facts: no
  tasks:
    - name: Test cli_command + network_cli + ce
      ansible.netcommon.cli_command:
        command: display clock
      register: result
    - debug: var=result.stdout
